package kew.providers.artemis.config.transport;

/**
 * Marker interface for {@link EndpointConfig endpoint configurations} where
 * it makes sense to use Artemis in-vm transport properties.
 * @see EmbeddedTransportProps
 */
public interface HasEmbeddedProps {

}
